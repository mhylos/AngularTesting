import { PrettyRutPipe } from './pretty-rut.pipe';

describe('PrettyRutPipe', () => {
  it('create an instance', () => {
    const pipe = new PrettyRutPipe();
    expect(pipe).toBeTruthy();
  });
});
